import { _ as _export_sfc, o as openBlock, c as createElementBlock, a2 as createStaticVNode } from "./chunks/framework.DqlImjjO.js";
const __pageData = JSON.parse('{"title":"并行与生命周期","description":"","frontmatter":{},"headers":[],"relativePath":"architecture/concurrency-lifecycle.md","filePath":"architecture/concurrency-lifecycle.md","lastUpdated":null}');
const _sfc_main = { name: "architecture/concurrency-lifecycle.md" };
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, [..._cache[0] || (_cache[0] = [
    createStaticVNode('<h1 id="并行与生命周期" tabindex="-1">并行与生命周期 <a class="header-anchor" href="#并行与生命周期" aria-label="Permalink to &quot;并行与生命周期&quot;">​</a></h1><p>HarnessVLN 的默认并行单位是<strong>完整 Task</strong>，不是 Task 内单步。并行策略同时受组件作用域、原生 资源限制和跨 Task 写入影响。</p><h2 id="runner-的有界并行" tabindex="-1">Runner 的有界并行 <a class="header-anchor" href="#runner-的有界并行" aria-label="Permalink to &quot;Runner 的有界并行&quot;">​</a></h2><p><code>BenchRunner</code> 使用一个容量为 <code>task_parallelism * 2</code> 的队列：producer 流式读取 <code>benchmark.cases()</code>， 多个 consumer 各自构造完整 stack 并调用 <code>run_task</code>。结果可能乱序完成，最后按输入 index 排序。</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Benchmark.cases() -&gt; bounded queue -&gt; worker 0 -&gt; whole Task A</span></span>\n<span class="line"><span>                                  -&gt; worker 1 -&gt; whole Task B</span></span>\n<span class="line"><span>                                  -&gt; worker 2 -&gt; whole Task C</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>该设计不会一次把百万级 split 全部装入内存，<code>max_cases</code> 也在 producer 侧截断。</p><p>Bench 级并行也有独立故障边界。一个 Bench 在构造、case stream 或调度前校验阶段失败时，App 将 异常收敛到该 <code>RunSummary.error</code>；同一 Runner 中其他 Bench 不被取消。只有 Runner/Agent 配置解析、 进程取消或 Manifest 写入这类全局失败才结束整个套件调用。</p><h2 id="task-内有哪些并发" tabindex="-1">Task 内有哪些并发 <a class="header-anchor" href="#task-内有哪些并发" aria-label="Permalink to &quot;Task 内有哪些并发&quot;">​</a></h2><p>一个 Task 内存在协程并发，但控制权仍属于 Agent：</p><ul><li><code>Agent.run</code> 与 <code>Environment.wait_terminal</code> 同时等待；</li><li>VLN Job 可以作为后台协程或独立 worker 进程运行；</li><li>RPC worker 可发出异步反向 tool call；</li><li>ToolBus 允许只读调用并行，并追踪在途写调用；</li><li>Memory 由自身实现决定是否并行查询。</li></ul><p>“Agent 与 VLN 同时运行”不意味着 Runner 分别调度它们。VLN 的生命周期由 navigator 管理， Agent 通过 Job 工具委托和观察状态。</p><h2 id="作用域与实例数" tabindex="-1">作用域与实例数 <a class="header-anchor" href="#作用域与实例数" aria-label="Permalink to &quot;作用域与实例数&quot;">​</a></h2><p>假设一个 Run 有 100 个 case：</p><table tabindex="0"><thead><tr><th>配置</th><th style="text-align:right;">Agent 实例</th><th style="text-align:right;">Env 实例</th><th style="text-align:right;">VLN 实例/worker</th><th style="text-align:right;">Memory 实例</th></tr></thead><tbody><tr><td>全部 task-scope</td><td style="text-align:right;">100</td><td style="text-align:right;">100</td><td style="text-align:right;">100</td><td style="text-align:right;">100</td></tr><tr><td>VLN <code>scope: run</code></td><td style="text-align:right;">100</td><td style="text-align:right;">100</td><td style="text-align:right;">1</td><td style="text-align:right;">100</td></tr></tbody></table><p>run-scoped VLN 在 case 之间解除旧 ToolClient 绑定、释放 Job media，再绑定新环境；checkpoint 和 worker PID 保持不变。Run 结束后 <code>ConfiguredStackFactory.close_run()</code> 负责关闭。</p><h2 id="两种串行约束" tabindex="-1">两种串行约束 <a class="header-anchor" href="#两种串行约束" aria-label="Permalink to &quot;两种串行约束&quot;">​</a></h2><p>同一 Bench 内的 <code>task_parallelism</code> 与 Runner 内的 <code>bench_parallelism</code> 不是同一个资源边界：</p><table tabindex="0"><thead><tr><th>条件</th><th>Task 并行</th><th>Bench 并行</th><th>原因</th></tr></thead><tbody><tr><td>VLN <code>scope: run</code></td><td>禁止</td><td>可用</td><td>每个 Bench 各有一个独立 worker，但同一 worker 不能绑定两个 Task</td></tr><tr><td>任一组件 <code>serial: true</code></td><td>禁止</td><td>禁止</td><td>表示进程级或外部共享资源独占</td></tr><tr><td>Memory <code>writeback: true</code></td><td>禁止</td><td>禁止</td><td>多实例会 read-modify-write 同一持久化文件</td></tr></tbody></table><p>StackFactory 用 <code>requires_serial</code> 告诉 <code>BenchRunner</code> 是否允许多个 Task；另用 <code>global_serial_reasons</code> 告诉 App 是否允许多个 Bench。冲突在任何 Bench 构造前预检，错误会列出 具体 factory 与原因。Isaac Kit 是典型的 <code>environment.serial</code> 资源；Dummy Landmark Memory 会 报告 <code>memory.serial</code> / <code>memory.writeback</code>。</p><p>纯 run-scoped VLN 不自动禁止 Bench 并行，因为每个 Bench 会创建自己的 navigator/worker。若两个 worker 仍会争用同一 GPU 或单例服务，应在 VLN 配置上再声明 <code>serial: true</code>。</p><div class="tip custom-block"><p class="custom-block-title">扩展并行的正确方式</p><p>需要多个 Isaac case 并行时，使用多个隔离服务进程并让每个 Environment 独占连接；需要共享 Memory 并行写时，实现事务型存储并关闭单文件 writeback。不要只删除 <code>serial</code> 标记。</p></div><h2 id="多频-vln-如何保留" tabindex="-1">多频 VLN 如何保留 <a class="header-anchor" href="#多频-vln-如何保留" aria-label="Permalink to &quot;多频 VLN 如何保留&quot;">​</a></h2><p>DualVLN 一类大小脑不同频模型不由 Harness 拆开。一个 Job 在 worker 内维护 System 1、System 2、 帧窗口与连续轨迹；worker 可在需要时主动 <code>nav.observe</code>，推理后调用多个动作或 <code>stand_still</code>。 外部只看到 Job 状态与审计事件。</p><p>这意味着时间尺度分三层：</p><ol><li>Runner 的 Task 调度频率；</li><li>Agent 的 workflow / Job 轮询频率；</li><li>VLN 内部推理与原生 Environment tick 频率。</li></ol><p>三层互不假定一一对应。Isaac 的一个 <code>nav.move.discrete</code> 还可能在中间件中推进最多数千 native tick， 直到高层动作完成。</p><h2 id="交互会话为何串行" tabindex="-1">交互会话为何串行 <a class="header-anchor" href="#交互会话为何串行" aria-label="Permalink to &quot;交互会话为何串行&quot;">​</a></h2><p><code>env</code> 模式没有 BenchRunner，但仍可能复用 run-scoped VLN。公共 <code>InteractiveNavigationSession.navigate()</code> 因此使用会话级锁，把“构造 Task、运行 Agent、完成 清理”作为一个不可重叠操作；并发提交的指令按锁顺序执行。<code>close()</code> 使用同一把锁，所以不会在 Environment 仍运动或 VLN Job 仍活跃时关闭共享 worker。</p><p>这是会话级顺序，不是 Runner 逐步控制。每个 Task 内依然由 Agent 自主调用工具，批量 <code>run</code> 模式 的 Task/Bench 并行也不受这把锁影响。</p><h2 id="取消不是简单-task-cancel" tabindex="-1">取消不是简单 <code>task.cancel()</code> <a class="header-anchor" href="#取消不是简单-task-cancel" aria-label="Permalink to &quot;取消不是简单 `task.cancel()`&quot;">​</a></h2><p>取消时可能存在 GPU 进程、子进程组、文件映射和在途运动。现有实现使用：</p><ul><li>shielded cancel/close，确保调用者被取消时清理仍能收敛；</li><li>worker process group 的 TERM/KILL 兜底与同步 fence；</li><li>Job seal，拒绝已关闭 Job 的新反向调用；</li><li>media release，按 Job 清理 file-backed array；</li><li>ToolBus write fence 与 drain；</li><li>独立记录 cleanup error，不覆盖主异常。</li></ul><p>编写新插件时，<code>stop()</code> 必须幂等、可在部分启动状态调用，并在返回前阻断后续副作用。</p>', 33)
  ])]);
}
const concurrencyLifecycle = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  concurrencyLifecycle as default
};
