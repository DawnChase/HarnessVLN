import { _ as _export_sfc, o as openBlock, c as createElementBlock, a2 as createStaticVNode } from "./chunks/framework.DqlImjjO.js";
const __pageData = JSON.parse('{"title":"插件契约总览","description":"","frontmatter":{},"headers":[],"relativePath":"extending/plugin-contract.md","filePath":"extending/plugin-contract.md","lastUpdated":null}');
const _sfc_main = { name: "extending/plugin-contract.md" };
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, [..._cache[0] || (_cache[0] = [
    createStaticVNode('<h1 id="插件契约总览" tabindex="-1">插件契约总览 <a class="header-anchor" href="#插件契约总览" aria-label="Permalink to &quot;插件契约总览&quot;">​</a></h1><p>新增模块前先选边界。HarnessVLN 不要求继承抽象基类；只要对象满足 Protocol，并通过 YAML factory 构造，就能接入。结构类型降低依赖，但也意味着 contract test 必须承担运行时保证。</p><h2 id="五类扩展点" tabindex="-1">五类扩展点 <a class="header-anchor" href="#五类扩展点" aria-label="Permalink to &quot;五类扩展点&quot;">​</a></h2><table tabindex="0"><thead><tr><th>扩展点</th><th>输入</th><th>输出/能力</th><th>生命周期</th></tr></thead><tbody><tr><td><code>NavigationAgent</code></td><td><code>NavContext</code></td><td>主动函数调用并最终 stop</td><td>Task</td></tr><tr><td><code>VLNNavigator</code></td><td>Task + 受限 ToolClient</td><td><code>vln.navigate.*</code> Job 工具</td><td>Task / Session</td></tr><tr><td><code>Environment</code></td><td><code>EnvironmentEpisode(task, setup)</code></td><td><code>nav.*</code> 工具、terminal、result</td><td>Task</td></tr><tr><td><code>SpatialMemory</code></td><td>Task + 受限 ToolClient</td><td><code>spatial.*</code> 工具</td><td>Task 实例，可持久化</td></tr><tr><td><code>Benchmark</code></td><td>dataset</td><td>Case stream 与 score</td><td>Bench session</td></tr></tbody></table><p>只有 Agent 与 Environment 构成最小运行 stack；VLN 和 Memory 都是可选能力。传统规划若与 simulator/navmesh 紧耦合，可直接由 Environment 注册工具，不需要新增核心组件类别。</p><h2 id="稳定面与扩展面" tabindex="-1">稳定面与扩展面 <a class="header-anchor" href="#稳定面与扩展面" aria-label="Permalink to &quot;稳定面与扩展面&quot;">​</a></h2><p><strong>稳定面</strong>：Protocol 方法、<code>NavTask/NavGoal/Observation/Pose</code>、工具名、现有 schema 语义、terminal 与清理顺序。</p><p><strong>扩展面</strong>：<code>public</code>、<code>channels</code>、<code>extras</code>、requirements 映射、Environment result、provenance、 插件 params。扩展字段能快速实验，但一旦两个以上插件依赖同一语义，应提升为有文档和测试的协议。</p><h2 id="接入前的四个问题" tabindex="-1">接入前的四个问题 <a class="header-anchor" href="#接入前的四个问题" aria-label="Permalink to &quot;接入前的四个问题&quot;">​</a></h2><ol><li><strong>谁决策？</strong> 组织步骤、选择工具和恢复策略属于 Agent；动作预测内部循环属于 VLN。</li><li><strong>谁拥有原生对象？</strong> simulator/service/robot 连接属于 Environment。</li><li><strong>谁拥有真值？</strong> dataset loader 与 evaluator 属于 Bench。</li><li><strong>状态活多久？</strong> Task 内状态放实例；重 checkpoint 可 session-scope；跨 Task 知识放 Memory 存储。</li></ol><h2 id="通用实现要求" tabindex="-1">通用实现要求 <a class="header-anchor" href="#通用实现要求" aria-label="Permalink to &quot;通用实现要求&quot;">​</a></h2><h3 id="目录" tabindex="-1">目录 <a class="header-anchor" href="#目录" aria-label="Permalink to &quot;目录&quot;">​</a></h3><ul><li><code>src/&lt;domain&gt;/*.py</code> 仅放被多个插件复用的基座；</li><li>具体插件放在 <code>src/&lt;domain&gt;/&lt;plugin&gt;/</code>，即使初版只有一个文件；</li><li>插件 <code>__init__.py</code> 只暴露稳定 factory 入口，内部文件可继续拆分；</li><li>新插件需通过 <code>tests/contract/test_plugin_layout.py</code>。</li></ul><h3 id="构造" tabindex="-1">构造 <a class="header-anchor" href="#构造" aria-label="Permalink to &quot;构造&quot;">​</a></h3><ul><li>factory 使用 <code>module:object</code>，参数可 YAML 化；</li><li>Environment factory 接受 <code>episode=EnvironmentEpisode</code>，不 import <code>BenchmarkCase</code>，也不读取 <code>truth</code>；</li><li>构造函数不应启动昂贵异步资源，启动放入 <code>start</code>；</li><li>参数错误尽早抛出具体异常。</li></ul><h3 id="能力声明" tabindex="-1">能力声明 <a class="header-anchor" href="#能力声明" aria-label="Permalink to &quot;能力声明&quot;">​</a></h3><ul><li>Agent/VLN/Memory 的 <code>required_tools</code> 只列实际调用项；</li><li>Environment 的 profile 精确写 channel、动作、frame、units 和 camera；</li><li>VLN requirements 只写模型硬要求，不写偏好；</li><li>新 Tool 必须 <code>additionalProperties: false</code>，给写操作标记 <code>writes=True</code>。</li></ul><h3 id="bench-评分" tabindex="-1">Bench 评分 <a class="header-anchor" href="#bench-评分" aria-label="Permalink to &quot;Bench 评分&quot;">​</a></h3><ul><li><code>score(case, result)</code> 只读取 Bench 私有 truth 和公开的 <code>NavigationResult</code>；</li><li>返回非空 <code>Mapping[str, Real]</code>，名称非空，值必须有限且不能是 bool；</li><li>缺少必需的环境结果时抛出带字段语义的异常，不返回 <code>NaN/Infinity</code>；</li><li>Executor 将合法值归一化为 float，将异常或非法返回值隔离为该 Case 的 <code>score</code> 错误。</li></ul><h3 id="生命周期" tabindex="-1">生命周期 <a class="header-anchor" href="#生命周期" aria-label="Permalink to &quot;生命周期&quot;">​</a></h3><ul><li>实例默认 single-use；</li><li><code>start()</code> 返回具体的 <code>Sequence[Tool]</code>，批次中只能包含 Tool，不能使用 generator 或重复名字；</li><li><code>stop</code> 幂等，处理未完全 start、异常、超时和取消；</li><li>Harness 在调用 <code>start</code> 前登记组件，因此部分 start 失败后仍会调用对应 <code>stop</code>；</li><li>后台 task 保留引用并在 stop 中 await；</li><li>外部进程按进程组回收；</li><li>stop 返回后不能再调用原生写操作。</li></ul><p><code>NavigationStack</code> 在生命周期开始前检查各角色的必需方法、<code>required_tools</code>、Environment profile 以及 VLN 工具所有权；VLN/Env requirement 组合也在 <code>Environment.start</code> 前校验。契约错误因此不会 先启动原生环境，未进入 start 的组件也不会收到 stop/result 调用。</p><h3 id="可观测性" tabindex="-1">可观测性 <a class="header-anchor" href="#可观测性" aria-label="Permalink to &quot;可观测性&quot;">​</a></h3><ul><li>业务状态通过 Job status、terminal、result 或 tool result 返回；</li><li>版本身份写入 <code>provenance</code>；</li><li>错误包含 case/job/tool 上下文，但不吞原异常类别；</li><li>大型逐步数据留在 runs，Git 中提交紧凑、可校验 trace。</li></ul><h2 id="最小测试金字塔" tabindex="-1">最小测试金字塔 <a class="header-anchor" href="#最小测试金字塔" aria-label="Permalink to &quot;最小测试金字塔&quot;">​</a></h2><table tabindex="0"><thead><tr><th>层级</th><th>必测内容</th></tr></thead><tbody><tr><td>unit</td><td>成功路径、非法参数、重复 stop、部分 start 失败、取消竞态</td></tr><tr><td>contract</td><td>factory 可加载、profile/requirements、dataset 公私边界、固定资源版本</td></tr><tr><td>integration</td><td>YAML -&gt; stack -&gt; 完整 Task -&gt; score -&gt; Manifest；非法指标不污染兄弟 Case</td></tr><tr><td>real smoke</td><td>真实 reset、观测、至少一个动作、goal finish、stop、无残留资源</td></tr><tr><td>parity</td><td>同版本官方 split/evaluator 指标对照</td></tr></tbody></table><h2 id="兼容性的判定" tabindex="-1">兼容性的判定 <a class="header-anchor" href="#兼容性的判定" aria-label="Permalink to &quot;兼容性的判定&quot;">​</a></h2><p>只有接口匹配不等于算法兼容。例如 R2R VLN 能调用 ObjectNav Environment 的 RGB-D 与离散动作， 但它没有类别目标训练和 prompt 语义，不能据此标记模型兼容。矩阵应分别记录：</p><ul><li><strong>mechanical compatibility</strong>：工具/profile 可组合；</li><li><strong>task compatibility</strong>：输入目标范式与模型能力匹配；</li><li><strong>validated compatibility</strong>：真实 evidence 与 evaluator 对照。</li></ul><h2 id="何时修改核心" tabindex="-1">何时修改核心 <a class="header-anchor" href="#何时修改核心" aria-label="Permalink to &quot;何时修改核心&quot;">​</a></h2><p>优先只新增插件。只有需求跨多个实现且无法用现有 tool/schema 表达时，才修改 ToolBus、runtime 或 公共 schema。核心改动必须补竞态测试与向后兼容说明，因为它的 blast radius 覆盖所有组合。</p>', 31)
  ])]);
}
const pluginContract = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  pluginContract as default
};
