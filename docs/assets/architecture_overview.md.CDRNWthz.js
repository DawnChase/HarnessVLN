import { _ as _imports_0 } from "./chunks/architecture-overview.BJWX6Gp4.js";
import { _ as _export_sfc, o as openBlock, c as createElementBlock, a2 as createStaticVNode } from "./chunks/framework.DqlImjjO.js";
const __pageData = JSON.parse('{"title":"总体分层","description":"","frontmatter":{},"headers":[],"relativePath":"architecture/overview.md","filePath":"architecture/overview.md","lastUpdated":null}');
const _sfc_main = { name: "architecture/overview.md" };
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, [..._cache[0] || (_cache[0] = [
    createStaticVNode('<h1 id="总体分层" tabindex="-1">总体分层 <a class="header-anchor" href="#总体分层" aria-label="Permalink to &quot;总体分层&quot;">​</a></h1><p>HarnessVLN 的架构目标不是制造一个包含所有导航范式的基类，而是让变化发生在正确边界内： Agent 组织能力，VLN 保留完整模型，Environment 适配执行系统，Bench 适配任务与评分。</p><p><img src="' + _imports_0 + '" alt="HarnessVLN 总体架构" class="architecture-asset"></p><h2 id="六层职责" tabindex="-1">六层职责 <a class="header-anchor" href="#六层职责" aria-label="Permalink to &quot;六层职责&quot;">​</a></h2><div class="layer-stack"><div class="layer-row" style="--layer-color:#4a7dab;"><strong>Bench / Runner</strong><p>Bench 流式产生 case 并评分；Runner 只调度完整 Task、收集有序结果。</p></div><div class="layer-row" style="--layer-color:#32806a;"><strong>Harness</strong><p>为一个 Task 装配组件、建立能力总线、仲裁终止并执行有界清理。</p></div><div class="layer-row" style="--layer-color:#176b58;"><strong>Agent Core</strong><p>唯一决策主体；固定 workflow 与自由 function-calling loop 都实现同一个 run 契约。</p></div><div class="layer-row" style="--layer-color:#a55258;"><strong>VLN / Memory</strong><p>作为 Agent 可委托的导航能力与空间状态能力，内部实现保持独立。</p></div><div class="layer-row" style="--layer-color:#665c9e;"><strong>ToolBus</strong><p>用名称、JSON Schema、actor 白名单、写属性和审计事件连接组件。</p></div><div class="layer-row" style="--layer-color:#39759f;"><strong>Environment</strong><p>把标准导航工具翻译为 Habitat、AI2-THOR、Isaac 或服务调用。</p></div></div><p><code>schemas/</code> 横跨这些层，但只保存小而稳定的数据结构：<code>NavTask</code>、<code>NavGoal</code>、<code>Observation</code>、 <code>Pose</code>、<code>EnvironmentTerminal</code> 和 <code>NavigationProfile</code>。模拟器原生类型不会进入 schema。</p><h2 id="运行时依赖方向" tabindex="-1">运行时依赖方向 <a class="header-anchor" href="#运行时依赖方向" aria-label="Permalink to &quot;运行时依赖方向&quot;">​</a></h2><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CLI -&gt; App -&gt; BenchRunner -&gt; NavigationHarness</span></span>\n<span class="line"><span> |      |          |              |</span></span>\n<span class="line"><span> |      |          |              +-&gt; Contracts + ToolBus + Schemas</span></span>\n<span class="line"><span> |      |          +-&gt; Benchmark</span></span>\n<span class="line"><span> |      +-&gt; ConfiguredStackFactory</span></span>\n<span class="line"><span> |                   |-- Agent</span></span>\n<span class="line"><span> |                   |-- Environment</span></span>\n<span class="line"><span> |                   |-- VLN</span></span>\n<span class="line"><span> |                   `-- Memory</span></span>\n<span class="line"><span> `-&gt; Manifest</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>插件可以依赖 <code>harness.contracts</code>、<code>harness.runtime.NavContext</code>、<code>harness.tool_bus.Tool</code> 和 <code>schemas</code>；核心层不反向 import 某个具体插件。唯一的具体绑定存在于 YAML factory。</p><h2 id="控制面与数据面" tabindex="-1">控制面与数据面 <a class="header-anchor" href="#控制面与数据面" aria-label="Permalink to &quot;控制面与数据面&quot;">​</a></h2><p><strong>控制面</strong>由 Runner、Harness、Job 状态和 terminal signal 组成，回答“谁启动、何时停止、怎样 回收”。<strong>数据面</strong>由 ToolBus 调用、Observation、动作结果和 memory item 组成，回答“导航期间 传了什么”。分开后，Runner 能批量运行而无需窥探逐步观测，VLN worker 能独立进程运行而不改变 Agent API。</p><h2 id="为什么不建立万能-basevln" tabindex="-1">为什么不建立万能 BaseVLN <a class="header-anchor" href="#为什么不建立万能-basevln" aria-label="Permalink to &quot;为什么不建立万能 BaseVLN&quot;">​</a></h2><p>StreamVLN、JanusVLN、DualVLN 的输入历史、输出格式、推理线程和 checkpoint 加载差异很大。 Harness 只要求它们：</p><ol><li>声明导航 profile requirements；</li><li>启动后注册 <code>vln.navigate.*</code> 工具；</li><li>Job 执行时只调用授权的导航工具；</li><li>停止时使后台任务和进程静默。</li></ol><p>这组边界足够连接，又不侵入模型内部。具体模型可以直接在进程内实现，也可继承现有 RPC navigator 把重依赖放进 worker。</p><h2 id="插件作用域" tabindex="-1">插件作用域 <a class="header-anchor" href="#插件作用域" aria-label="Permalink to &quot;插件作用域&quot;">​</a></h2><table tabindex="0"><thead><tr><th>组件</th><th>支持作用域</th><th>原因</th></tr></thead><tbody><tr><td>Benchmark</td><td>task-only factory lifecycle</td><td>一个 Run 创建一次 Bench，由它流式产生 case</td></tr><tr><td>Agent</td><td><code>task</code></td><td>工作流与对话历史不能跨 case 泄漏</td></tr><tr><td>Environment</td><td><code>task</code></td><td>原生 session 和场景状态按 Task 隔离</td></tr><tr><td>Memory</td><td><code>task</code> 实例，可持久化文件</td><td>连接短寿命，知识可跨 Task</td></tr><tr><td>VLN</td><td><code>task</code> 或 <code>run</code></td><td>大模型可跨 case 复用 worker/checkpoint</td></tr></tbody></table><p>run-scoped VLN 当前要求串行 case，防止同一 worker 被两个环境同时绑定。未来若 worker 明确支持 多租户，应新增资源池实现，而不是放松现有不变量。</p><h2 id="扩展点不是所有内部函数" tabindex="-1">扩展点不是所有内部函数 <a class="header-anchor" href="#扩展点不是所有内部函数" aria-label="Permalink to &quot;扩展点不是所有内部函数&quot;">​</a></h2><p>首要公开扩展点只有五个：<code>NavigationAgent</code>、<code>VLNNavigator</code>、<code>Environment</code>、<code>SpatialMemory</code> 和 <code>Benchmark</code>。Tool 名与公共 schema 是它们之间的协议。Runner、terminal arbitration、RPC transport、Manifest writer 属于基础设施；只有出现跨插件的真实需求时才扩大接口。</p><p>继续阅读：<a href="./task-model.html">Task 与 Goal 模型</a>和<a href="./../extending/plugin-contract.html">插件契约总览</a>。</p>', 21)
  ])]);
}
const overview = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  overview as default
};
