import { _ as _export_sfc, o as openBlock, c as createElementBlock, a2 as createStaticVNode } from "./chunks/framework.DqlImjjO.js";
const __pageData = JSON.parse('{"title":"设计取舍","description":"","frontmatter":{},"headers":[],"relativePath":"reference/design-decisions.md","filePath":"reference/design-decisions.md","lastUpdated":null}');
const _sfc_main = { name: "reference/design-decisions.md" };
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, [..._cache[0] || (_cache[0] = [
    createStaticVNode('<h1 id="设计取舍" tabindex="-1">设计取舍 <a class="header-anchor" href="#设计取舍" aria-label="Permalink to &quot;设计取舍&quot;">​</a></h1><p>本页记录当前架构刻意做出的选择，以及暂时没有引入的复杂度。它帮助后续扩展判断某个限制是遗漏， 还是为了保护边界设置的不变量。</p><h2 id="agent-主导-而非-runner-主导" tabindex="-1">Agent 主导，而非 Runner 主导 <a class="header-anchor" href="#agent-主导-而非-runner-主导" aria-label="Permalink to &quot;Agent 主导，而非 Runner 主导&quot;">​</a></h2><div class="decision-grid"><div class="decision-card"><strong>选择</strong><p>Runner 只启动完整 Task；每个 Task 只调用一次 Agent.run。</p></div><div class="decision-card"><strong>代价</strong><p>Agent 必须正确处理循环、预算、Goal 与 stop，不能依赖 Runner 兜底逐步推进。</p></div></div><p>原因是不同 VLN 有不同内部频率与状态。逐步 Runner 会强迫所有模型降格为一步 policy，并阻碍自由 function-calling Agent。Harness 仍负责 timeout 和生命周期，但不夺取决策权。</p><h2 id="agent-与-vln-分开" tabindex="-1">Agent 与 VLN 分开 <a class="header-anchor" href="#agent-与-vln-分开" aria-label="Permalink to &quot;Agent 与 VLN 分开&quot;">​</a></h2><p>Agent 组织工具与工作流；VLN 保留完整模型导航循环。透传 Agent 很薄，但它仍处理 Job terminal、 复合 Goal 与 Task stop。这样同一 VLN 可被固定 workflow 或自由 Agent 使用，同一 Agent 也可换模型。</p><h2 id="bench-与-environment-分开" tabindex="-1">Bench 与 Environment 分开 <a class="header-anchor" href="#bench-与-environment-分开" aria-label="Permalink to &quot;Bench 与 Environment 分开&quot;">​</a></h2><p>Bench 持有 loader/truth/score，Environment 持有 native session/action/result。共享 simulator 的 R2R、 ObjectNav 与 GOAT 可复用中间件，同时保持各自数据与评分语义。新 simulator adapter 必须绑定具体 Bench 才能谈兼容。</p><h2 id="小-protocol-可扩展映射" tabindex="-1">小 Protocol + 可扩展映射 <a class="header-anchor" href="#小-protocol-可扩展映射" aria-label="Permalink to &quot;小 Protocol + 可扩展映射&quot;">​</a></h2><p>公共数据类只固定身份、指令、时间、坐标框架和 observation envelope；模态 channel 与 plugin requirement 使用映射。这样 v0.1 能扩展而不预先设计万能 schema。代价是扩展字段需要文档、版本与 contract test，不能任意堆键。</p><h2 id="toolbus-而非直接对象引用" tabindex="-1">ToolBus，而非直接对象引用 <a class="header-anchor" href="#toolbus-而非直接对象引用" aria-label="Permalink to &quot;ToolBus，而非直接对象引用&quot;">​</a></h2><p>ToolBus 让能力可发现、可校验、可授权、可审计，并能在停止时封写。额外开销是 JSON Schema 校验 和显式工具定义；导航仿真/模型推理的成本远高于这部分，换来的竞态边界值得保留。</p><h2 id="vln-job-而非一步-predict" tabindex="-1">VLN Job，而非一步 <code>predict</code> <a class="header-anchor" href="#vln-job-而非一步-predict" aria-label="Permalink to &quot;VLN Job，而非一步 `predict`&quot;">​</a></h2><p>Job 允许模型自主多次 observe/move，适配视频模型、异步大小脑和连续轨迹。Agent 通过 start/status/ cancel 管理委托。代价是必须认真处理 Job 生命周期、反向调用和媒体释放。</p><h2 id="task-scope-默认-vln-可-run-scope" tabindex="-1">task-scope 默认，VLN 可 run-scope <a class="header-anchor" href="#task-scope-默认-vln-可-run-scope" aria-label="Permalink to &quot;task-scope 默认，VLN 可 run-scope&quot;">​</a></h2><p>Agent/Environment 按 Task 隔离，避免状态泄漏。大模型 checkpoint 加载昂贵，允许 VLN worker 跨 case 复用，但当前强制串行并在任务间 detach tools/release media。若需要并行，下一步是 worker pool，而非共享一个 navigator。</p><h2 id="memory-实例短寿命-存储可长寿" tabindex="-1">Memory 实例短寿命，存储可长寿 <a class="header-anchor" href="#memory-实例短寿命-存储可长寿" aria-label="Permalink to &quot;Memory 实例短寿命，存储可长寿&quot;">​</a></h2><p>Task-scope 实例使 ToolClient 和任务上下文清晰；原子文件/数据库使知识跨 Task。基础 Dummy Memory 只验证边界，不提前引入向量库、地图服务和 evolution pipeline。</p><h2 id="当前不做自进化层" tabindex="-1">当前不做自进化层 <a class="header-anchor" href="#当前不做自进化层" aria-label="Permalink to &quot;当前不做自进化层&quot;">​</a></h2><p>基础版本聚焦导航闭环、空间记忆和兼容矩阵。记忆压缩、经验提炼、策略更新可能分别属于存储维护、 Agent workflow 或训练系统，过早合成一个 <code>evolution</code> 插件会隐藏触发条件和评测污染风险。真实研究 需求明确后，再按输入、输出、时机、可见性与 rollback 设计独立协议。</p><h2 id="yaml-factory-而非安装式插件注册" tabindex="-1">YAML factory，而非安装式插件注册 <a class="header-anchor" href="#yaml-factory-而非安装式插件注册" aria-label="Permalink to &quot;YAML factory，而非安装式插件注册&quot;">​</a></h2><p>项目无需 package install 或中央 registry。<code>PYTHONPATH=src</code> 配合 <code>module:object</code> factory 足以快速 迭代；不同模型仍按子目录隔离。代价是结构类型错误在加载/启动期发现，因此配置与 contract test 必须严格。</p><h2 id="一个-conda-环境-进程隔离依赖" tabindex="-1">一个 Conda 环境，进程隔离依赖 <a class="header-anchor" href="#一个-conda-环境-进程隔离依赖" aria-label="Permalink to &quot;一个 Conda 环境，进程隔离依赖&quot;">​</a></h2><p>首发追求一次环境创建能运行 Agent、模型与模拟器，避免用多个 Python 环境掩盖版本冲突。worker 进程用于 CUDA/生命周期隔离，不用于切换依赖集合。若未来两个后端存在不可调和 ABI，服务边界应 显式版本化，而不是让 Harness API 感知环境管理细节。</p><h2 id="明确保守的兼容声明" tabindex="-1">明确保守的兼容声明 <a class="header-anchor" href="#明确保守的兼容声明" aria-label="Permalink to &quot;明确保守的兼容声明&quot;">​</a></h2><p>adapter、data contract、真实 smoke、固定 trace 和官方 parity 分开记录。保守声明会让矩阵看起来 进展更慢，但它防止接口存在被误读为指标可信，是科研 Harness 必须保留的证据标准。</p>', 27)
  ])]);
}
const designDecisions = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  designDecisions as default
};
